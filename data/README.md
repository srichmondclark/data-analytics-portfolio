# Data

This directory contains sample datasets used across multiple projects.

## Data Organization

- Store raw datasets in their original format
- Use descriptive filenames
- Include data source information in project documentation
- Consider data privacy and licensing requirements

## Supported Formats

- CSV files (`.csv`)
- Excel files (`.xlsx`, `.xls`)
- R data files (`.rds`, `.rda`)
